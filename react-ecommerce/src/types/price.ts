export type PriceData = {
  price: number;
  discountPercentage: number;
  quantity: number;
};

export type prdPriceDetails = {
  priceAmount: number;
  discountAmount: number;
  saleAmount: number;
  totalAmount: number;
};
